<UserControl
    x:Class="UWP_Timer.Controls.IconButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP_Timer.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="root"
    Height="60"
    Width="60"
    d:DesignHeight="60"
    d:DesignWidth="40">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <FontIcon Glyph="{Binding Icon,ElementName=root}" FontSize="{Binding FontSize,ElementName=root}"/>
        <TextBlock Text="{Binding Label,ElementName=root}" VerticalAlignment="Center" Grid.Row="1" FontSize="{Binding FontSize,ElementName=root}"/>

    </Grid>
</UserControl>
