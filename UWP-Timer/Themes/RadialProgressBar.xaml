<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local2="using:UWP_Timer.Controls"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml">

    <Style TargetType="local2:RadialProgressBar">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local2:RadialProgressBar">
                    <Grid>
                        <canvas:CanvasControl x:Name="Canvas"/>
                        <TextBlock x:Name="ValueTb" FontSize="60" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
